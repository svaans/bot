[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11",
    "maturin>=1.4",
]
build-backend = "setuptools.build_meta"

[tool.maturin]
bindings = "pyo3"
module-name = "fast_indicators_rust.fast_indicators_rust"